#include <stdio.h>
#include "main.h"
/**
 * main- passes two arguments argc , argv
 * @argc:counts the no. of args being passed to main
 * @argv:the array of pointers containing strings
 * Return:0 (success)
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
